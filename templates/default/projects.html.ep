% layout 'common';
  % $home =~ s/^\///;
  <div class="page_header">
    <a href="<%= url_for('projects')->query({home => $home}); %>">Projects</a>
    <%= '/' . $home %>
  </div>
  <table class="project_list">
    <a href="<%= url_for '/' %>">homes</a>
    <tr>
      <th>Project</th>
      <th>Description</th>
      <th>Owner</th>
      <th>Last Change</th>
      <th></th>
    </tr>
    % for my $project (@$projects) {
      % my $name = "$home/" . $project->{path};
      <tr class="dark">
        <td><a class="list" href="<%= url_for('summary', project => $name) %>"><%= $project->{path} %></a></td>
        <td><a class="list" title="<%= $project->{descr_long} %>" href="<%= url_for('/summary')->query(home => param('home'), project => $project->{path}) %>"><%= $project->{descr} %></a></td>
        <td><i><%= $project->{owner} %></i></td>
        <td class="age2"><%= $project->{age_string} %></td>
        <td class="link">
          % my $pname = $project->{path};
          <a href="<%= url_for('summary', project => $name) %>">summary</a> |
          <a href="<%= url_for('/shortlog')->query({home => $home, project => $pname}) %>">shortlog</a> |
          <a href="<%= url_for('/log')->query({home => $home, project => $pname}) %>">log</a> |
          <a href="<%= url_for('/tree')->query({home => $home, project => $pname}) %>">tree</a>
        </td>
      </tr>
    % }
  </table>
  <div class="page_footer"></div>
