% layout 'common';
  <div class="page_header">
    <a href="/">Home</a>
  </div>
  <table class="project_list">
    % for my $home (@$homes) {
      <tr class="dark">
        <td>
          <a class="list" href="<%= url_for('project', home => gitweblite_rel($home)); %>">
            <%= $home %>
          </a>
        </td>
    % }
  </table>
