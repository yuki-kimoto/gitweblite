% layout 'common';
  <div class="page_header">
    <a href="/">Roots</a> /
  </div>
  <table class="project_list">
    <tr>
      <th>Roots</th>
    </tr>
    % for my $projectroot (@$projectroots) {
      <tr class="dark">
        <td>
          <a class="list" href="<%= url_for('/projects')->query(root => $projectroot) %>">
            <%= $projectroot %>
          </a>
        </td>
    % }
  </table>
