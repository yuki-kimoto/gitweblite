% use File::Basename 'basename';
% my $project = stash('project');
% $project = basename $project if defined $project;

<div class="page_header">
  <a href="<%= $logo_link %>">
    <img src="<%= url_for('/git-logo.png') %>" class="logo"/>
  </a>
  <a href="#"><%= $title %></a> <%= $project ? "# $project" : '' %>
</div>
